server:
  forward-headers-strategy: native
  port: 8086
spring:
  application:
    name: sc-admin-server
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://config:config@localhost:8888