server:
  port: 8085
  forward-headers-strategy: framework
spring:
  application:
    name: sc-feedback-service
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://config:config@localhost:8888