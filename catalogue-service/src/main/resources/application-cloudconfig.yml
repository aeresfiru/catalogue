server:
  port: 8081
  forward-headers-strategy: framework
spring:
  application:
    name: sc-catalogue-service
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://config:config@localhost:8888