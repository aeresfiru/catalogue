spring:
  datasource:
    url: jdbc:tc:postgresql:15.2-alpine:///sc?TC_DAEMON=true
    username: sc
    password: sc
  boot:
    admin:
      client:
        enabled: false
  cloud:
    config:
      enabled: false
eureka:
  client:
    enabled: false
