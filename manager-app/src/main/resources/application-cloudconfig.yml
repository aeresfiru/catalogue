server:
  port: 8084
  forward-headers-strategy: framework
spring:
  application:
    name: sc-manager-app
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://config:config-password@config-server:8888