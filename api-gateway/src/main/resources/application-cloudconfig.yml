server:
  port: 8087
  forward-headers-strategy: framework
spring:
  application:
    name: sc-api-gateway
  config:
    import: 'configserver:'
  cloud:
    config:
      uri: http://config:config@localhost:8888